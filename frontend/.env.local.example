# URL of your Express backend (local dev vs deployed Railway URL)
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Personal-use stand-in for real auth — the same user id you created in
# Prisma Studio when testing the backend. Swap for real auth later.
NEXT_PUBLIC_USER_ID=""
